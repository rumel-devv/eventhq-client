import DashboardHeading from "@/components/DashboardHeading";
import React from "react";

const AddEventPage = () => {
  return (
    <div>
      <DashboardHeading
        title="Add Event"
        description="Fill in the event details, select the venue and date, configure ticket pricing, and publish your event so attendees can discover and join it."
      />
    </div>
  );
};

export default AddEventPage;
